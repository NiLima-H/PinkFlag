version: "3.9"

services:
  queuestorm:
    build: .
    container_name: queuestorm-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
